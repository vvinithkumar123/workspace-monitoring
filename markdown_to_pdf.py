from markdown2 import markdown\nfrom weasyprint import HTML\nimport os\n\ndef convert_markdown_to_pdf(markdown_file, pdf_file):\n    # Read the Markdown content from the file\n    with open(markdown_file, 'r') as f:\n        markdown_content = f.read()\n\n    # Convert Markdown to HTML\n    html_content = markdown(markdown_content)\n\n    # Add a navigable table of contents\n    html_content = '<h1>Table of Contents</h1><ul>' + \n                   ''.join(f'<li><a href="#{section}">{section}</a></li>' for section in markdown_content.split('\n') if section.startswith('#')) + \n                   '</ul>' + html_content\n\n    # Convert HTML to PDF\n    HTML(string=html_content).write_pdf(pdf_file)\n\nif __name__ == "__main__":\n    readme_file = 'readme.md'\n    output_pdf_file = 'output.pdf'\n    convert_markdown_to_pdf(readme_file, output_pdf_file)\n